// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/paycrest/aggregator/ent/predicate"
	"github.com/paycrest/aggregator/ent/providerbankaccount"
	"github.com/paycrest/aggregator/ent/providerprofile"
)

// ProviderBankAccountQuery is the builder for querying ProviderBankAccount entities.
type ProviderBankAccountQuery struct {
	config
	ctx          *QueryContext
	order        []providerbankaccount.OrderOption
	inters       []Interceptor
	predicates   []predicate.ProviderBankAccount
	withProvider *ProviderProfileQuery
	withFKs      bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ProviderBankAccountQuery builder.
func (pbaq *ProviderBankAccountQuery) Where(ps ...predicate.ProviderBankAccount) *ProviderBankAccountQuery {
	pbaq.predicates = append(pbaq.predicates, ps...)
	return pbaq
}

// Limit the number of records to be returned by this query.
func (pbaq *ProviderBankAccountQuery) Limit(limit int) *ProviderBankAccountQuery {
	pbaq.ctx.Limit = &limit
	return pbaq
}

// Offset to start from.
func (pbaq *ProviderBankAccountQuery) Offset(offset int) *ProviderBankAccountQuery {
	pbaq.ctx.Offset = &offset
	return pbaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pbaq *ProviderBankAccountQuery) Unique(unique bool) *ProviderBankAccountQuery {
	pbaq.ctx.Unique = &unique
	return pbaq
}

// Order specifies how the records should be ordered.
func (pbaq *ProviderBankAccountQuery) Order(o ...providerbankaccount.OrderOption) *ProviderBankAccountQuery {
	pbaq.order = append(pbaq.order, o...)
	return pbaq
}

// QueryProvider chains the current query on the "provider" edge.
func (pbaq *ProviderBankAccountQuery) QueryProvider() *ProviderProfileQuery {
	query := (&ProviderProfileClient{config: pbaq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pbaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pbaq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(providerbankaccount.Table, providerbankaccount.FieldID, selector),
			sqlgraph.To(providerprofile.Table, providerprofile.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, providerbankaccount.ProviderTable, providerbankaccount.ProviderColumn),
		)
		fromU = sqlgraph.SetNeighbors(pbaq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ProviderBankAccount entity from the query.
// Returns a *NotFoundError when no ProviderBankAccount was found.
func (pbaq *ProviderBankAccountQuery) First(ctx context.Context) (*ProviderBankAccount, error) {
	nodes, err := pbaq.Limit(1).All(setContextOp(ctx, pbaq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{providerbankaccount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) FirstX(ctx context.Context) *ProviderBankAccount {
	node, err := pbaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ProviderBankAccount ID from the query.
// Returns a *NotFoundError when no ProviderBankAccount ID was found.
func (pbaq *ProviderBankAccountQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pbaq.Limit(1).IDs(setContextOp(ctx, pbaq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{providerbankaccount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := pbaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ProviderBankAccount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ProviderBankAccount entity is found.
// Returns a *NotFoundError when no ProviderBankAccount entities are found.
func (pbaq *ProviderBankAccountQuery) Only(ctx context.Context) (*ProviderBankAccount, error) {
	nodes, err := pbaq.Limit(2).All(setContextOp(ctx, pbaq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{providerbankaccount.Label}
	default:
		return nil, &NotSingularError{providerbankaccount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) OnlyX(ctx context.Context) *ProviderBankAccount {
	node, err := pbaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ProviderBankAccount ID in the query.
// Returns a *NotSingularError when more than one ProviderBankAccount ID is found.
// Returns a *NotFoundError when no entities are found.
func (pbaq *ProviderBankAccountQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = pbaq.Limit(2).IDs(setContextOp(ctx, pbaq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{providerbankaccount.Label}
	default:
		err = &NotSingularError{providerbankaccount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := pbaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ProviderBankAccounts.
func (pbaq *ProviderBankAccountQuery) All(ctx context.Context) ([]*ProviderBankAccount, error) {
	ctx = setContextOp(ctx, pbaq.ctx, ent.OpQueryAll)
	if err := pbaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ProviderBankAccount, *ProviderBankAccountQuery]()
	return withInterceptors[[]*ProviderBankAccount](ctx, pbaq, qr, pbaq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) AllX(ctx context.Context) []*ProviderBankAccount {
	nodes, err := pbaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ProviderBankAccount IDs.
func (pbaq *ProviderBankAccountQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if pbaq.ctx.Unique == nil && pbaq.path != nil {
		pbaq.Unique(true)
	}
	ctx = setContextOp(ctx, pbaq.ctx, ent.OpQueryIDs)
	if err = pbaq.Select(providerbankaccount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := pbaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pbaq *ProviderBankAccountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pbaq.ctx, ent.OpQueryCount)
	if err := pbaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pbaq, querierCount[*ProviderBankAccountQuery](), pbaq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) CountX(ctx context.Context) int {
	count, err := pbaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pbaq *ProviderBankAccountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pbaq.ctx, ent.OpQueryExist)
	switch _, err := pbaq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pbaq *ProviderBankAccountQuery) ExistX(ctx context.Context) bool {
	exist, err := pbaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ProviderBankAccountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pbaq *ProviderBankAccountQuery) Clone() *ProviderBankAccountQuery {
	if pbaq == nil {
		return nil
	}
	return &ProviderBankAccountQuery{
		config:       pbaq.config,
		ctx:          pbaq.ctx.Clone(),
		order:        append([]providerbankaccount.OrderOption{}, pbaq.order...),
		inters:       append([]Interceptor{}, pbaq.inters...),
		predicates:   append([]predicate.ProviderBankAccount{}, pbaq.predicates...),
		withProvider: pbaq.withProvider.Clone(),
		// clone intermediate query.
		sql:  pbaq.sql.Clone(),
		path: pbaq.path,
	}
}

// WithProvider tells the query-builder to eager-load the nodes that are connected to
// the "provider" edge. The optional arguments are used to configure the query builder of the edge.
func (pbaq *ProviderBankAccountQuery) WithProvider(opts ...func(*ProviderProfileQuery)) *ProviderBankAccountQuery {
	query := (&ProviderProfileClient{config: pbaq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pbaq.withProvider = query
	return pbaq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Institution string `json:"institution,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ProviderBankAccount.Query().
//		GroupBy(providerbankaccount.FieldInstitution).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pbaq *ProviderBankAccountQuery) GroupBy(field string, fields ...string) *ProviderBankAccountGroupBy {
	pbaq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ProviderBankAccountGroupBy{build: pbaq}
	grbuild.flds = &pbaq.ctx.Fields
	grbuild.label = providerbankaccount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Institution string `json:"institution,omitempty"`
//	}
//
//	client.ProviderBankAccount.Query().
//		Select(providerbankaccount.FieldInstitution).
//		Scan(ctx, &v)
func (pbaq *ProviderBankAccountQuery) Select(fields ...string) *ProviderBankAccountSelect {
	pbaq.ctx.Fields = append(pbaq.ctx.Fields, fields...)
	sbuild := &ProviderBankAccountSelect{ProviderBankAccountQuery: pbaq}
	sbuild.label = providerbankaccount.Label
	sbuild.flds, sbuild.scan = &pbaq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ProviderBankAccountSelect configured with the given aggregations.
func (pbaq *ProviderBankAccountQuery) Aggregate(fns ...AggregateFunc) *ProviderBankAccountSelect {
	return pbaq.Select().Aggregate(fns...)
}

func (pbaq *ProviderBankAccountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pbaq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pbaq); err != nil {
				return err
			}
		}
	}
	for _, f := range pbaq.ctx.Fields {
		if !providerbankaccount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pbaq.path != nil {
		prev, err := pbaq.path(ctx)
		if err != nil {
			return err
		}
		pbaq.sql = prev
	}
	return nil
}

func (pbaq *ProviderBankAccountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ProviderBankAccount, error) {
	var (
		nodes       = []*ProviderBankAccount{}
		withFKs     = pbaq.withFKs
		_spec       = pbaq.querySpec()
		loadedTypes = [1]bool{
			pbaq.withProvider != nil,
		}
	)
	if pbaq.withProvider != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, providerbankaccount.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ProviderBankAccount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ProviderBankAccount{config: pbaq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pbaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pbaq.withProvider; query != nil {
		if err := pbaq.loadProvider(ctx, query, nodes, nil,
			func(n *ProviderBankAccount, e *ProviderProfile) { n.Edges.Provider = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pbaq *ProviderBankAccountQuery) loadProvider(ctx context.Context, query *ProviderProfileQuery, nodes []*ProviderBankAccount, init func(*ProviderBankAccount), assign func(*ProviderBankAccount, *ProviderProfile)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ProviderBankAccount)
	for i := range nodes {
		if nodes[i].provider_profile_provider_bank_accounts == nil {
			continue
		}
		fk := *nodes[i].provider_profile_provider_bank_accounts
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(providerprofile.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "provider_profile_provider_bank_accounts" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pbaq *ProviderBankAccountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pbaq.querySpec()
	_spec.Node.Columns = pbaq.ctx.Fields
	if len(pbaq.ctx.Fields) > 0 {
		_spec.Unique = pbaq.ctx.Unique != nil && *pbaq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pbaq.driver, _spec)
}

func (pbaq *ProviderBankAccountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(providerbankaccount.Table, providerbankaccount.Columns, sqlgraph.NewFieldSpec(providerbankaccount.FieldID, field.TypeUUID))
	_spec.From = pbaq.sql
	if unique := pbaq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pbaq.path != nil {
		_spec.Unique = true
	}
	if fields := pbaq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, providerbankaccount.FieldID)
		for i := range fields {
			if fields[i] != providerbankaccount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pbaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pbaq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pbaq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pbaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pbaq *ProviderBankAccountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pbaq.driver.Dialect())
	t1 := builder.Table(providerbankaccount.Table)
	columns := pbaq.ctx.Fields
	if len(columns) == 0 {
		columns = providerbankaccount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pbaq.sql != nil {
		selector = pbaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pbaq.ctx.Unique != nil && *pbaq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pbaq.predicates {
		p(selector)
	}
	for _, p := range pbaq.order {
		p(selector)
	}
	if offset := pbaq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pbaq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ProviderBankAccountGroupBy is the group-by builder for ProviderBankAccount entities.
type ProviderBankAccountGroupBy struct {
	selector
	build *ProviderBankAccountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pbagb *ProviderBankAccountGroupBy) Aggregate(fns ...AggregateFunc) *ProviderBankAccountGroupBy {
	pbagb.fns = append(pbagb.fns, fns...)
	return pbagb
}

// Scan applies the selector query and scans the result into the given value.
func (pbagb *ProviderBankAccountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbagb.build.ctx, ent.OpQueryGroupBy)
	if err := pbagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProviderBankAccountQuery, *ProviderBankAccountGroupBy](ctx, pbagb.build, pbagb, pbagb.build.inters, v)
}

func (pbagb *ProviderBankAccountGroupBy) sqlScan(ctx context.Context, root *ProviderBankAccountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pbagb.fns))
	for _, fn := range pbagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pbagb.flds)+len(pbagb.fns))
		for _, f := range *pbagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pbagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ProviderBankAccountSelect is the builder for selecting fields of ProviderBankAccount entities.
type ProviderBankAccountSelect struct {
	*ProviderBankAccountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pbas *ProviderBankAccountSelect) Aggregate(fns ...AggregateFunc) *ProviderBankAccountSelect {
	pbas.fns = append(pbas.fns, fns...)
	return pbas
}

// Scan applies the selector query and scans the result into the given value.
func (pbas *ProviderBankAccountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pbas.ctx, ent.OpQuerySelect)
	if err := pbas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ProviderBankAccountQuery, *ProviderBankAccountSelect](ctx, pbas.ProviderBankAccountQuery, pbas, pbas.inters, v)
}

func (pbas *ProviderBankAccountSelect) sqlScan(ctx context.Context, root *ProviderBankAccountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pbas.fns))
	for _, fn := range pbas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pbas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pbas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
